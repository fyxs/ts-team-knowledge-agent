# Agent 入口

本文件是项目 Agent 的最小入口，不承载具体编码规范。

开始任务前必须阅读：

1. `agent/README.md`
2. `agent/engineering-rules.md`
3. `agent/workflow.md`
4. 与当前任务相关的 `docs/` 文档
5. 涉及界面、样式或交互时，必须先读 `design/README.md`

所有约束 Agent 进行编码、测试、调试、审查和提交的规范，统一维护在 `agent/` 目录。除入口索引外，不要在本文件重复定义规则。
